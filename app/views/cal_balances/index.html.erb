
<div class="container my-5">
  <h4>カロリーバランス</h4>
   <div class="row text-center d-flex align-items-center my-5">
     <div class="col">
        <% if @cal_ingestion.present? == true || @cal_consumption.present? == true %>
        <%= l @cal_ingestion.created_at %>
      <% else %>
        <%= Date.current.strftime("%m月%d日(%a) %H:%M") %>
      <% end %>
    </div>
  </div>
   <div class="row text-center d-flex align-items-center my-5">
     <div class="col">
    <span><%= @cal_ingestion&.total_cal_ingestions.to_i - @cal_consumption&.total_cal_consumptions.to_i %> Kcal</span>
     </div>
   </div>
    <div class="row text-center d-flex align-items-center my-5">
      <div class="col-6">
        カロリー摂取量
      </div>
       <div class="col-6">
        カロリー消費量
      </div>
    </div>
    <div class="row text-center d-flex align-items-center my-5">
       <div class="col-6">
         <!--カロリ-摂取量-->
      <span>+  </span><%= @cal_ingestion&.total_cal_ingestions.to_i %><span>Kcal</span>
      </div>
      <div class="col-6">
        <!--カロリ-消費量-->
        <span>-  </span><%= @cal_consumption&.total_cal_consumptions.to_i %><span> Kcal</span>
      </div>
    </div>
    <div class="row text-center d-flex align-items-center my-5">
      <div class="col-6">
      <% if @cal_ingestion.present? == false %>
        <%=link_to "カロリー摂取量入力",new_cal_ingestion_path %>
      <% else %>
        <%=link_to "カロリー摂取量入力",edit_cal_ingestion_path(@cal_ingestion.id) %>
      <% end %>
      </div>
      <div class="col-6">
      <% if @cal_consumption.present? == false %>
        <%=link_to "カロリー消費量入力",new_cal_consumption_path %>
      <% else %>
        <%=link_to "カロリー消費量入力",edit_cal_consumption_path(@cal_consumption.id) %>
      <% end %>
      </div>
    </div>
